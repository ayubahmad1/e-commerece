<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @product.name %>
</p>

<p>
  <strong>Description:</strong>
  <%= @product.description %>
</p>

<p>
  <strong>Quantity:</strong>
  <%= @product.quantity %>
</p>

<p>
  <strong>Price:</strong>
  <%= @product.price %>
</p>


<%= render @product.comments %>


<%= render 'comments/form' if policy(@product).comment? %>

<%= link_to 'Edit', edit_product_path(@product) if policy(@product).update? %> |
<%= link_to 'Back', root_path %>
